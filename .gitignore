# --- Terraform 관련 ---
.terraform/                # 테라폼 플러그인 폴더 (용량 큼)
*.tfstate                  # 인프라 상태 파일 (보안 민감!)
*.tfstate.backup           # 백업 파일
*.tfvars                   # 로컬 변수 설정 파일 (민감 정보 포함 가능)
tfplan                     # 테라폼 플랜 결과물

# --- Ansible 관련 ---
k3s-ansible/inventory/hosts.yml   # GHA가 실시간으로 만드는 파일이므로 무시
*.retry                           # 플레이북 실패 시 생기는 임시 파일

# --- OS & 시스템 관련 ---
.DS_Store                  # Mac 전용 숨김 파일
*.log                      # 로그 파일들
.env                       # 환경 변수 파일
